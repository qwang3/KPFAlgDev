import numpy as np

from common import macro as mc
from common.argument import spec as sp

class TFA:

    def __init__(self, temp: type(sp.Spec), obs: type(sp.Spec)):
        '''

        '''
    def run(self, m:int) -> type(ArgRes):
        '''

        '''
    def common_range(self, order: int) -> mc.EchellePair_TYPE:
        '''
        
        '''
    def compute_dif(self, spec: mc.EchellePair_TYPE) -> mc.EchellePair_TYPE:
        '''

        '''
    def solve_step(self, R: mc.EchelleData_TYPE) -> mc.ALPHA_TYPE:
        '''

        '''
    def solve_order(self, order: int) -> ArgRes: 
        '''

        '''
    def solve_all(self) -> ArgRes:
        '''
        
        '''